<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC 
"-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="qna">
	<select id="getQnaList" resultType="com.spring3.dto.Qna">
		select * from qna order by resdate desc
	</select>	
	<select id="getCustom" resultType="com.spring3.dto.Qna">
		select * from qna where id=#{id}
	</select>
	<select id="maxNum" resultType="int">
		select count(*) from qna
	</select>
	<insert id="insCustom">
		insert into custom values (#{id}, #{pw},#{name},#{email},#{tel}, #{addr}, #{postcode}, default)
	</insert>
	<update id="changePw">
		update custom set pw=#{pw} where id=#{id}
	</update>
	<update id="changeInfo">
		update custom set name=#{name},email=#{email}, tel=#{tel}, addr=#{addr}, postcode=#{postcode}, where id=#{id}
	</update>
	<delete id="delCustom"> 
		delete from custom where id=#{#id}
	</delete>
</mapper>

<!-- 해당 요소의 id는 중복되어서도 안되며, 메소드 이름을 의미합니다. -->
<!-- 
·	private int no;
	private int plevel;
	private int parno;
	private String title;
	private String content;
	private String resdate;
	private int visited;
	private String aid;
	-->